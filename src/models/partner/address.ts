export default interface Address {
  id: number;
  idPersonalData: number;
  street: string;
  addessNumber: string;
  addressDetail: string;
  cep: string;
  district: string;
  state: string;
  city: string;
}
